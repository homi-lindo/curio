const appDisplayName = 'Curió';
// Keep the package identity stable so existing installs, data, and WACK
// evidence remain tied to the same Windows app registration.
const appWindowsAppUserModelId = 'App.Lume.Personal';
const appUriScheme = 'lume';
